# Local development secrets for running the engine OUTSIDE docker.
# Copy to `secrets.toml` and adjust. NEVER commit the real `secrets.toml`.
# (Inside docker-compose these values come from infrastructure/.env instead.)

[storage]
access_key = "minioadmin"
secret_key = "minioadmin"

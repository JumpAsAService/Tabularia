Tabularia {{ .Chart.AppVersion }} is being deployed as "{{ .Release.Name }}".

{{ if .Values.ingress.enabled -}}
Open:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else -}}
No Ingress was created. Reach the UI with:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "tabularia.fullname" . }}-frontend {{ .Values.service.frontendPort }}:{{ .Values.service.frontendPort }}
{{- end }}

Sign in with {{ .Values.auth.adminEmail }} and the password from
{{ if .Values.existingSecret }}the Secret "{{ .Values.existingSecret }}" (key AUTH__ADMIN_PASSWORD){{ else }}secrets.adminPassword{{ end }}.
That account is seeded only on the first start; it is never reset afterwards.

Check the rollout:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}

Two components are single-replica BY DESIGN and the chart does not let you scale
them — the gateway (its scheduler takes no distributed lock) and beat (N clocks
enqueue every periodic job N times). Everything else scales freely.
{{ if not .Values.networkPolicy.enabled }}
NOTE: networkPolicy is disabled. The engine API is reachable from any pod in the
cluster, and the workers can connect anywhere the cluster can reach. See
README.md → Known limits before going to production.
{{- end }}
{{- if .Values.oidc.enabled }}

NOTE: single sign-on is enabled. Admission is open by default — everyone the
provider authenticates gets an account. Set oidc.allowedEmailDomains or
oidc.requireAllowlistedGroup if your issuer is the whole company directory.
{{- end }}

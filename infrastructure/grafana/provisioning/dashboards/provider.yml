# Carica le dashboard JSON dalla cartella montata.
apiVersion: 1

providers:
  - name: dataprep
    orgId: 1
    folder: "Data Prep"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
